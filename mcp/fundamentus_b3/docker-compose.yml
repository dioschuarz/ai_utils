services:
  fundamentus-mcp:
    build:
      context: .
    container_name: fundamentus-mcp
    restart: unless-stopped
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "8101:8000"
    networks:
      - investment-net

networks:
  investment-net:
    external: true

